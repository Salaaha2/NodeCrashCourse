const { people, ages } = require('./people') //imported the file to modules
// Only able to print out with the console.log in peoples 

console.log(people, ages)

const = os require('os')

console.log(os.platforn(), os.homdir()) // pltform of os and home directory name of os 