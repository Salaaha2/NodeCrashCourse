const people = ['Alykaa', 'Jim', 'Dave', 'Allen']
const ages = [24, 25, 30, 35]

console.log(people)

module.exports = {
    // people: people // adds prop of people and set equal to the people constant
    people, ages
}; //export 